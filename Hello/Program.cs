using System;

namespace Hello
{
    class Program
    {
        /*
            Multi-line comment
            Everyhthing enclosed here
            is ignored by the compiler
         */

         // Single line comment
        static void Main(string[] args) // Main entry point
        {
            // Console.WriteLine() Prints text to the screen, then reads from a new line
            // Which means it prints a an empty line after the text (\n)
            Console.WriteLine("Hello World!");
        }
    }
}
